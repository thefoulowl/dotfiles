# goforit EQ profile: Flat (passthrough; used as the base for boost)
context.properties = { log.level = 0 }
context.spa-libs = { audio.convert.* = audioconvert/libspa-audioconvert  support.* = support/libspa-support }
context.modules = [
  { name = libpipewire-module-rt args = { nice.level = -11 } flags = [ ifexists nofail ] }
  { name = libpipewire-module-protocol-native }
  { name = libpipewire-module-client-node }
  { name = libpipewire-module-adapter }
  { name = libpipewire-module-filter-chain
    args = {
      node.description = "Flat"
      media.name       = "Flat"
      filter.graph = {
        nodes = [
          { type = builtin label = copy name = thru control = { } }
        ]
        links = [
        ]
      }
      audio.channels = 2
      audio.position = [ FL FR ]
      capture.props  = { node.name = "eq_flat" media.class = Audio/Sink }
      playback.props = { node.name = "eq_flat_out" node.passive = true target.object = "@TARGET@" }
    }
  }
]
